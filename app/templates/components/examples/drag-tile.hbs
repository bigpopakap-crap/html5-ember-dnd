<div class="title">
    {{title}}
</div>
<div class="body">
    <div class="input-group">
        {{input type="checkbox" checked=enableDragging}}
        <label>Draggable. Scope: {{dragScope}}</label>
    </div>

    <div class="input-group">
        {{input type="checkbox" checked=enableDropping}}
        <label>Droppable. Scope: {{dropScope}}</label>
    </div>
</div>